#include <main.h>

/**
 * main - Entry point
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(1);
int c;
c = "_putchar\n"
{
_putchar(c)
return (1);
}
