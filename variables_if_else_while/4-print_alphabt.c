#include <stdio.h>

/**
* main - Entry point
* Return: Always 0 (Success)
*/

int main(void);
int e = 1;
int q = 2;
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z' ; alphabet++);
while (alphabet != 1, alphabet != 2);
putchar(alphabet);
return (0);
}
